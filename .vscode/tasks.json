{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "CppBuild",
      "command": "cd ${fileDirname} && mkdir -p ./out/ && clang++ -std=c++11 -g ${file} -o ${fileDirname}/out/${fileBasenameNoExtension}",
      "args": [],
      "options": {
        "cwd": "/usr/bin"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}